{
  "engine": "playwright",
  "title": "should get js page content",
  "actions": [
    { "action": "goto", "args": ["/"] },
    {
      "action": "extract",
      "args": [{
        "type": "string",
        "selector": "#results"
      }]
    }
  ],
  "expected": {
    "html": { "contains": "<pre id=\"results\">{\"webdriver\":false" }
  }
}
