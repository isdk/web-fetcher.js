{
  "name": "089-mouse-keyboard-degrade",
  "title": "Verify mouse and keyboard actions throw not_supported error in cheerio mode",
  "engine": "cheerio",
  "actions": [
    { "action": "goto", "params": { "url": "/" } },
    { "action": "mouseMove", "params": { "selector": "#status" } }
  ],
  "expected": {
    "error": {
      "code": "not_supported"
    }
  }
}
