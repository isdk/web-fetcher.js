{
  "title": "waitFor should throw when failOnTimeout is default (true)",
  "engine": "playwright",
  "options": {
    "timeoutMs": 1000
  },
  "actions": [
    {
      "action": "goto",
      "params": {
        "url": "/"
      }
    },
    {
      "action": "waitFor",
      "params": {
        "selector": "#non-existent",
        "ms": 1000
      }
    }
  ],
  "expected": {
    "error": "Timeout"
  }
}
